buildscript {
    repositories {
        mavenCentral()
        google()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.1.3'
    }
}

ext {
    compileSdkVersion = 28
    buildToolsVersion = "28.0.0"
}

allprojects {
    repositories {
        mavenCentral()
        google()
    }
}
